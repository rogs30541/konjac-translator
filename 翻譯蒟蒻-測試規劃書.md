# 翻譯蒟蒻 — 測試規劃書

> 對應《系統規劃書》四階段開發計畫與《UI 規劃書》介面規格
> 版本:v0.1|日期:2026-07-13

---

## 一、測試策略總覽

本專案有兩種本質不同的待測物,策略必須分開:

| 待測物 | 特性 | 策略 |
|--------|------|------|
| **軟體行為**(API、UI、資料流、轉發) | 對就是對、錯就是錯 | 傳統測試金字塔:單元 → 整合 → E2E,CI 全自動 |
| **AI 品質**(辨識、翻譯、講者、摘要) | 沒有絕對正確,只有好壞程度 | 評測集(benchmark)+ 量化指標 + 回歸門檻,模型/參數變動時執行 |

### 測試金字塔配置

```
        ┌─────────────┐
        │  E2E(少)   │  Playwright:桌面 App / Chrome 擴充 / NotebookLM 轉發
        ├─────────────┤
        │ 整合(中)   │  FastAPI TestClient + WebSocket、引擎管線串接、DB
        ├─────────────┤
        │ 單元(多)   │  pytest(Python 引擎)、vitest(React)、cargo test(Rust)
        ├─────────────┤
        │ AI 評測(獨立軌)│  WER/CER、翻譯品質、DER、摘要 rubric — 不進每次 CI
        └─────────────┘
```

### 核心原則
1. **音訊不進 CI 用真機收音**:所有自動化測試用「預錄音訊 fixtures」餵管線;真實裝置收音只在手動驗收清單。
2. **AI 指標是回歸門檻,不是絕對標準**:先跑基準線(baseline),之後任何模型升級、prompt 修改、參數調整,都必須不低於基準線的容忍範圍。
3. **NotebookLM 自動化視為「必然會壞」**:用合約測試 + 每日排程探測,壞掉時要能在使用者遇到之前知道。

---

## 二、測試環境矩陣

| 環境 | 硬體 | 用途 |
|------|------|------|
| Windows + NVIDIA GPU | RTX 4060 8GB(開發機) | 主要測試環境:CUDA 路徑、WASAPI Loopback |
| Windows CPU only | 同機關閉 CUDA | 純 CPU 降級路徑、低配警告 UI |
| macOS Apple Silicon | M2 16GB(若有) | Metal 路徑、BlackHole 引導流程;無機器則列為社群測試項 |
| 瀏覽器 | Chrome(主)、Edge | 擴充套件;Firefox/Safari 不在支援範圍(MV3 tabCapture) |
| CI(GitHub Actions) | ubuntu / windows runner,無 GPU | 單元 + 整合 + UI E2E(mock 引擎);AI 評測不進 CI |
| AI 評測機 | 開發機 GPU,手動或夜間排程 | 評測集全量跑 |

---

## 三、測試資料集(Fixtures)

### 3.1 音訊評測集(自建,約 60–90 分鐘總量)

| 編號 | 內容 | 語言 | 講者 | 用途 |
|------|------|------|------|------|
| FX-01 | 乾淨朗讀(新聞稿) | 中/英/日 各 5 分 | 1 | ASR 基準線 |
| FX-02 | 雙人對話(自錄腳本會議) | 英 10 分 | 2 | 講者辨識 + 即時管線 |
| FX-03 | 三人會議(含插話、重疊語音) | 中英夾雜 10 分 | 3 | 講者辨識壓力、code-switching |
| FX-04 | 帶背景噪音(鍵盤、冷氣、街聲) | 英 5 分 | 1 | 噪音強健性 |
| FX-05 | 專業術語段落(軟體/醫療/財務各一) | 英 6 分 | 1 | 「會議主題上下文」翻譯效果 A/B |
| FX-06 | 長音檔(公開演講,CC 授權) | 英 60 分 | 1 | 離線批次、長時穩定性、記憶體 |
| FX-07 | 靜音/純音樂/極短檔(<2 秒)/損壞檔 | — | — | 邊界與錯誤處理 |

每個 fixture 附**人工校對的黃金標準**:逐字稿(ground truth)、講者分段標記、參考譯文。這是一次性投資,之後所有回歸都靠它。

> 注意:FX 檔案自行錄製或使用明確開放授權素材(LibriSpeech、Common Voice、CC 演講),不得使用真實會議錄音,避免隱私與授權問題。

### 3.2 軟體測試 fixtures
- 假 WebSocket 字幕流腳本(給前端測試,不需引擎)。
- Mock Ollama server(固定回應,測翻譯/摘要管線的錯誤處理與逾時)。
- NotebookLM 頁面 DOM 快照(給擴充 selector 合約測試)。

---

## 四、AI 品質評測(獨立軌)

### 4.1 語音辨識(ASR)
- **指標**:英文 WER(word error rate)、中文 CER(character error rate)、日文 CER。
- **方法**:`jiwer` 對評測集逐檔計算,按 fixture 分組出報告。
- **基準線**:Phase 1 完成時以 faster-whisper large-v3 在 FX-01~05 跑出首次數值,寫入 `benchmarks/baseline.json`。
- **回歸門檻**:任何變更後,各組指標劣化不得超過基準線 +10%(相對值);超過即擋下合併。
- **附加檢查**:時間戳偏移(對齊黃金標準 ±1 秒內比例)、幻覺偵測(靜音段不得產出文字 — 用 FX-07 驗證)。

### 4.2 翻譯品質
- **主指標**:chrF / COMET 分數(對 FX-01、FX-05 的參考譯文)。
- **輔助**:LLM-as-judge(用固定 prompt 讓另一個 LLM 對 100 句抽樣評 1–5 分:準確性/流暢度/術語),同一 judge 模型與 prompt 固定版本,分數只做趨勢比較。
- **A/B 專項**:FX-05 加/不加「會議主題上下文」各跑一次,驗證術語翻譯確有提升(這是系統規劃書的賣點,必須有數據支撐)。
- **降級路徑**:NLLB 離線翻譯單獨建基準線(預期低於 LLM,但不得低於可用底線 — 首輪人工評定)。

### 4.3 講者辨識
- **指標**:DER(diarization error rate,用 `pyannote.metrics` 計算)、講者數量判定正確率。
- **關鍵情境**:FX-03 重疊語音段的表現單獨列出;2 人/3 人場景分開統計。
- **升級驗證**:Phase 4 從 resemblyzer 換 pyannote 3.x 時,同一評測集對比,DER 需顯著改善才切換預設。

### 4.4 AI 摘要
- **方法**:rubric 人工評分 + LLM-as-judge 雙軌,每版模型/prompt 對評測集會議跑一次。
- **Rubric(1–5 分 × 5 維)**:重點覆蓋率、無捏造內容(hallucination)、待辦事項正確歸屬講者、結構可讀性、語言正確(繁中)。
- **紅線**:「無捏造」低於 4 分即不得發布 — 摘要寫出逐字稿沒有的決議是本產品最嚴重的信任事故。
- **校正逐字稿**:LLM 修正後的稿與原稿 diff,抽查修正是否引入新錯誤。

### 4.5 效能與延遲
| 指標 | 目標(依系統規劃書) | 測法 |
|------|---------------------|------|
| 即時端到端延遲(音訊→譯文顯示) | RTX 4060 ≤ 1.5s(p95);CPU 模式 ≤ 5s | 在 fixture 播放管線埋時戳,自動統計 |
| 離線處理速度 | 60 分音檔:GPU ≤ 10 分 | FX-06 計時 |
| 長時穩定 | 連續 4 小時即時模式:不崩潰、記憶體成長 < 300MB、延遲不遞增 | 夜間 soak test,循環播放 fixtures |
| 引擎冷啟動 | App 啟動到引擎就緒 ≤ 15s(模型已下載) | 桌面 E2E 計時 |

> 以上目標值以 jt-live-whisper 公開實測值為推估起點,Phase 1 實測後校準為正式門檻。

---

## 五、軟體測試(依模組)

### 5.1 核心引擎(Python / FastAPI)— pytest

**單元測試**
- 音訊分段(VAD 切分邏輯):給定波形,斷言切分點。
- 翻譯模式路由:十種模式 × Ollama 可用/不可用 → 斷言走 LLM 或 NLLB 備援。
- 講者分群後處理:分群結果 → 顏色/標籤分配穩定性(同一講者不換色)。
- 紀錄層:SQLite CRUD、全文搜尋、講者改名後全文與摘要同步更新。
- Markdown 匯出:摘要在前/逐字稿在後、時戳與講者格式符合 NotebookLM 規格(快照測試)。
- 錯誤處理:損壞音檔、不支援格式、模型檔缺失、Ollama 逾時 → 明確錯誤碼,不得 crash。

**整合測試**
- REST:session 生命週期(建立→錄製→停止→查詢→匯出→刪除)。
- WebSocket:連線 → 餵 FX-02 → 斷言字幕事件序列(partial → final、講者事件、順序不亂)。
- 斷線重連:WebSocket 中斷後重連,字幕不丟失不重複。
- 併發:同時 1 個即時 session + 2 個離線任務,互不干擾。
- 遠端 GPU 模式:mock remote server,驗證 failover(遠端掛掉退回本機)。

### 5.2 桌面 App(Tauri)— vitest + WebDriver/Playwright

**單元(vitest + Testing Library)**
- 字幕卡片元件:原文/譯文/講者色票/streaming 游標各狀態渲染。
- 假 WebSocket 流 → 字幕列表滾動、專注模式切換、講者改名 UI。

**E2E(tauri-driver / Playwright)**
- App 啟動 → 自動拉起引擎(sidecar)→ 狀態列顯示已連線。
- 全域快捷鍵:Ctrl+Shift+R 開始/停止、Ctrl+Shift+M 標記(需真桌面環境,Windows runner)。
- 懸浮字幕:開啟/拖曳/置頂/點擊穿透模式(點擊穿透需手動驗收)。
- 隱私:設定保留 7 天 → 造舊檔 → 觸發清理 → 斷言只刪音檔不刪逐字稿。
- 引擎崩潰復原:kill 引擎程序 → App 顯示斷線 → 自動重啟 → 恢復。

### 5.3 Chrome 擴充 — Playwright(載入 unpacked extension)

- tabCapture 授權流程:點「擷取此分頁」→ 權限對話 → 音訊流建立。
- 側欄字幕流:mock 引擎 WebSocket → 斷言雙語字幕渲染與 streaming 狀態。
- 降級模式:引擎連不上 → 黃色警示橫幅 + Web Speech 模式標示(Web Speech 本身在 CI 用 mock)。
- 頁面字幕疊加:content script 注入 YouTube 測試頁,字幕條出現且不擋播放器控制列。
- 引擎探測:輪詢 `127.0.0.1` 引擎,啟動/關閉引擎時側欄狀態即時切換。

### 5.4 NotebookLM 轉發(重點風險區)

| 層級 | 測試 | 頻率 |
|------|------|------|
| 合約測試 | 對 NotebookLM DOM 快照驗證 selector 仍可定位「新增來源」等元素;快照定期更新 | 每次 CI |
| 線上探測(canary) | 排程用測試 Google 帳號實跑一次「開筆記本→新增文字來源→驗證出現」 | 每日 |
| 降級測試 | 模擬自動化失敗 → 斷言 fallback:開頁 + 內容進剪貼簿 + 引導 UI 出現 | 每次 CI |
| 防重複 | 同 session 轉發兩次 → 第二次出現「已轉發」確認提示 | 每次 CI |
| Enterprise API | mock GCP endpoint 驗證 batchCreate 請求格式;有測試專案時做真連線 smoke | 每次 CI / 手動 |
| notebooklm-py 路徑 | 標記為選配插件:僅煙霧測試,失敗不擋版 | 每週 |

### 5.5 安全與隱私測試
- 引擎只綁定 `127.0.0.1`,區網掃描確認 WebUI 埠未預設對外(WebUI 對外需明確開啟)。
- 音檔與 DB 存放於使用者目錄,解除安裝引導提示資料位置。
- 「一鍵刪除全部資料」後,磁碟不殘留音檔/DB/暫存。
- Chrome 擴充權限最小化審查:僅 `tabCapture`、`sidePanel`、`storage`、NotebookLM 網域的 host permission。
- 無遙測:封包側錄確認除使用者觸發的轉發外,無任何對外連線(Ollama/模型下載除外且需明示)。

---

## 六、手動驗收清單(每次發版)

自動化蓋不到、必須真人真機的項目:

1. 真實 Google Meet 會議 30 分鐘:雙向模式,字幕可讀性、延遲體感、講者分色正確。
2. 系統音訊裝置切換(拔插耳機、切輸出裝置)不中斷錄製。
3. 懸浮字幕在簡報全螢幕、遊戲全螢幕上的置頂表現。
4. macOS BlackHole 首次安裝引導全流程(若支援 macOS)。
5. 中英夾雜實際對話的翻譯體感抽查。
6. NotebookLM 真帳號轉發 → 在 NotebookLM 內提問,引用能對回逐字稿時戳。
7. 低配機(無 GPU 筆電)冷啟動與 CPU 模式可用性。

---

## 七、CI/CD 與測試節奏

| 觸發 | 執行內容 | 門檻 |
|------|----------|------|
| 每次 PR | lint + 單元 + 整合(mock 引擎/Ollama)+ 擴充 E2E + NotebookLM 合約/降級測試 | 全綠才可合併 |
| 每日排程 | NotebookLM canary、soak test(4h)、依賴安全掃描 | 失敗發通知 |
| 模型/prompt/AI 參數變更 | AI 評測全量(WER/CER、翻譯、DER、摘要 rubric) | 不低於基準線容忍範圍 |
| 發版前 | 全平台安裝包 smoke(乾淨 VM 安裝→精靈→首次轉錄)+ 手動驗收清單 | 清單全過 |

工具鏈:GitHub Actions(ubuntu:Python/前端;windows runner:Tauri E2E)、`pytest` + `pytest-asyncio`、`vitest`、`Playwright`、`jiwer`、`pyannote.metrics`、評測報告輸出到 `benchmarks/` 目錄版本化追蹤。

---

## 八、各階段測試交付(對應系統規劃書 Phase)

### Phase 1 — 引擎服務化
- [ ] 評測集 FX-01~07 錄製完成 + 黃金標準校對
- [ ] pytest 單元/整合套件,覆蓋率 ≥ 70%(管線核心邏輯)
- [ ] AI 基準線首跑:`benchmarks/baseline.json`(WER/CER、DER、翻譯、延遲)
- [ ] WebSocket 字幕流整合測試綠燈
- **出口準則**:同一 fixture 在新 API 與原 jt-live-whisper 輸出等值(等值 = 逐字稿一致、時戳 ±1s)

### Phase 2 — 桌面 App
- [ ] vitest 元件測試 + Tauri E2E(啟動、快捷鍵、引擎生命週期、隱私清理)
- [ ] 4 小時 soak test 通過
- [ ] 手動驗收清單 1、2、3、7
- **出口準則**:乾淨 Windows VM 從安裝到看見第一句字幕 ≤ 10 分鐘(含模型下載除外)

### Phase 3 — Chrome 擴充
- [ ] Playwright 擴充套件全套 + NotebookLM 合約/canary/降級測試上線
- [ ] 手動驗收 6(真帳號轉發 + NotebookLM 內提問溯源)
- **出口準則**:Google Meet 全程字幕 → 一鍵摘要 → 轉發 NotebookLM,全流程無手動介入

### Phase 4 — 進階功能
- [ ] pyannote 對比評測(DER 改善才切換)
- [ ] Supabase 多人同步:雙瀏覽器即時性測試(字幕同步 ≤ 2s)
- [ ] Telegram/Slack/Discord 轉發整合測試(mock webhook)

---

## 九、風險與測試對策

| 風險 | 測試對策 |
|------|----------|
| NotebookLM UI 改版 | 合約測試 + 每日 canary,壞掉 24h 內知道;降級路徑常測常綠 |
| Whisper 幻覺(靜音生字) | FX-07 靜音組零輸出斷言列為固定回歸 |
| 摘要捏造內容 | rubric 紅線 4 分 + 發版前人工抽查;此項永不放寬 |
| GPU CI 缺失 | AI 評測固定在開發機/夜間排程跑,結果 json 入版控,PR 引用最近一次結果 |
| 音訊裝置差異大 | 自動化全走 fixtures;裝置相容性靠手動清單 + 使用者回報模板(附診斷資訊匯出鈕) |
| 三端元件漂移 | 字幕卡片元件庫共用 + 快照測試,三端同一份 |

---

*本規劃書與《系統規劃書》《UI 規劃書》同步維護;基準線數值於 Phase 1 實測後回填。*
